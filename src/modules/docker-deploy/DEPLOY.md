# Deploy en 1 comando (módulo docker-deploy)

```bash
docker compose up -d
```

Servicios: `app` (3000), `whatsapp-bridge` (3001), `mailpit` (8025), Postgres Supabase local (54322).

Variables en `.env` en la raíz del proyecto.
